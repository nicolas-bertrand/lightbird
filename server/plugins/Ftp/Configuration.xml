<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <contexts>
    <context>
      <transport>TCP</transport>
      <protocol>FTP</protocol>
    </context>
    <context>
      <transport>TCP</transport>
      <protocol>FTP-DATA</protocol>
    </context>
  </contexts>
  <!-- The maximum size send at a time to the client. -->
  <maxPacketSize>10000000</maxPacketSize>
</configuration>
